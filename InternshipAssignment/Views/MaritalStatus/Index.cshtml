@model IEnumerable<InternshipAssignment.Models.MaritalStatus>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.MaritalStatusName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.EntryDate)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MaritalStatusName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EntryDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaritalStatusId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaritalStatusId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaritalStatusId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
